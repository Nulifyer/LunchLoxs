{
  "name": "todo-frontend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/dev-server.ts",
    "build": "bun build src/index.ts --outdir dist --minify && bun build src/workers/service-worker.ts --outdir dist --minify && bun build src/workers/db-worker.ts --outdir dist --minify && bun run scripts/copy-public.ts",
    "preview": "bunx serve dist"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.3.0",
    "@connectrpc/connect": "^2.0.2",
    "@connectrpc/connect-web": "^2.0.2",
    "@sqlite.org/sqlite-wasm": "3.51.2-build8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
